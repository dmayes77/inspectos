// Seed data for development — add real sample services here if needed
export const services: {
  name: string;
  description?: string | null;
  category?: string | null;
  price?: number | null;
  durationMinutes?: number | null;
  status?: string;
  templateId?: string | null;
}[] = [];
