// Seed data for development — add real sample leads here if needed
export function getCrmLeads(): {
  name: string;
  email?: string | null;
  stage: string;
  service?: string | null;
  requestedDate?: string | null;
  value?: number | null;
}[] {
  return [];
}
